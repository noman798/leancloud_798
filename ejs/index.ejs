<!DOCTYPE html>

<head>
<meta charset=utf-8>
<title><%= title %></title>
<meta name="description" content="<%= description %>">
<link rel=canonical href="http://<%= default_host %>">
</head>

<body>

<h1><%= title %></h1>
<h2><%= description %></h2>

<div>
<% for (var i = 0; i < li.length; ++i) { %>
    <div>
        <a href=""></a> 
    </div> 
<% } %>
</div>
<div>
    <a href="/next/{post.ID}">下一页</a>
</div>
</body>

